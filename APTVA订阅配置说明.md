# APTV 订阅配置说明

> 最近更新：2026-07-26 · **央视优选 v3（国内骨干源，真机实测候选）**

## 一、订阅链接（只要央视）

在 APTV 里选「设置 → 播放列表 → 添加」，填下面任意一条：

| 用途 | 链接 |
|---|---|
| **央视优选 v3（推荐）** | `https://py8008my.github.io/iptv-sub/cctv.m3u` |
| 中文同名（需编码） | `https://py8008my.github.io/iptv-sub/aptv_cctv%E4%BC%98%E9%80%89.m3u` |
| jsDelivr 加速 | `https://cdn.jsdelivr.net/gh/py8008my/iptv-sub@main/cctv.m3u` |

EPG 节目单：`https://epg.112114.xyz/pp.xml`（已内嵌在订阅里）

> 全量版：`https://py8008my.github.io/iptv-sub/index.m3u`

## 二、为什么是这一版（踩过的坑）

1. **纯官方央视源 = 有声无画**：tv.cctv.com 对大部分台走 ChinaDRM 加密（`cdrmldcctvN_1`），视频轨加密、音频轨明文，APTV 这类第三方播放器拿不到密钥 → 你之前看到的"光有声音没画面"。体育台（CCTV-5/5+/16）尤其如此。
2. **GITV / 银河 源 = Token 失效**：`gitv.tv` 的 `livodToken` 是 7 天有效期，旧列表里的 token 早已过期，请求返回 HTML 报错页，无法播放 → **本版已全部剔除**。
3. **沙箱在境外，无法测中国延迟**：生成环境在海外，境外 VPS 源测起来飞快但你在国内必卡死；国内 CDN 源在沙箱常超时，其实你那边能用。**所以"按沙箱延迟删源"是反向操作**，本版改为按"地址归属（是否国内）"筛选，不依赖沙箱连通性。

## 三、v3 怎么选源

全部 18 个央视频道，每个 1~3 条线路，按以下优先级排列（你能直接看到每条属于哪类）：

| 优先级 | 来源类型 | 说明 |
|---|---|---|
| 1 | 浙江广电 CDN（cztv.com） | **已实测可达 260ms、1080p 有画面**，作为 CCTV-13 的锚定首源 |
| 2 | 央视官方非 DRM 接口 | cctv.cn / cctv.com（部分可能仍带 DRM，留意"有声无画"） |
| 3 | 国内广电 / 运营商 CDN | 省级广电、移动/联通域名 |
| 4 | 国内骨干 IP 转发 | 联通/电信/移动骨干 IP（如 `123.160.x` 联通河南、`58.248.x` 电信广东、`112.x` 移动）——**长城宽带走联通骨干，这类最可能稳** |

已剔除：境外 VPS（38/63/74/198 等）、IPv6（你网络无 IPv6）、GITV 失效 token、个人/海外域名。

## 四、你需要做的事：真机验收

沙箱测不了中国网络，**最终能不能播、卡不卡，只有你的设备说了算**。请照这个步骤验收：

1. APTV 里添加上面 `cctv.m3u` 订阅。
2. 逐个点开 18 个央视频道，记录情况，分三类：
   - ✅ 有画面有声音
   - ⏳ 转圈/超时（卡死）
   - 🔇 有声音没画面（DRM）
3. 把结果告诉我（哪个台 + 哪种情况），我据此**删掉失效线路、补更稳的源**。

预期：CCTV-13（cztv）最稳；其余频道靠国内骨干 IP 转发，大部分应能出画，少数可能因 `Authinfo` 过期或运营商限制需要我补线。

## 五、文件清单

| 文件 | 说明 |
|---|---|
| `cctv.m3u` | 央视优选 v3 主订阅（18 台，英文名便于订阅） |
| `aptv_cctv优选.m3u` / `aptv_cctv官方.m3u` | 同上内容的备份 |
| `build_cctv_v3.py` | v3 生成器（按地址归属筛选，剔除失效源） |
| `verify_cctv.py` | 沙箱 ffprobe 实测脚本（仅用于海外可达源验证，仅供参考） |
| `cctv_test_report.json` | 沙箱实测明细（仅 3 条海外可达，不具中国代表性） |
